# ── Output: Pipeline timing profiler ──────────────────────────────────────────
# Enables per-stage latency recording.  Output path is set by --pipeline-timing-path.
# Combine with output/standard_ppm.yaml for full observability.

pipeline_timing_enabled: true
